# Loaded by `npm run build:demo` (vite --mode demo). Routes every API call in src/lib/api.ts to
# the in-browser mock in src/lib/demoApi.ts instead of a real backend -- see App.tsx and
# demoApi.ts for what that does and does not cover.
VITE_DEMO_MODE=true
